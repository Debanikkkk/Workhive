export interface ReqEmployee {
    firstName?: string,
    lastName?: string,
    status?: boolean,
    username?: string,
    password?: string,
    department?: number,
    branch?: number,
    company?: number,
    role?: number,
}